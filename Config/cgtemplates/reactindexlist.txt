#module_IndexList


import { connect } from 'react-redux'
import List{{modulePageName}}Container from './list{{modulePageNamePlural}}Container'

const mapDispatchToProps = (dispatch) => {
  return {
    
  }
}

const mapStateToProps = (state) => {
  return {
  
  }
}

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(List{{modulePageName}}Container)

#endmodule_IndexList
